<!DOCTYPE html>
html(lang="en")
  head
    title= site.title
  body
    h1= page.title
    h2= page.slogan
    h3
      a(href='/about/') About
    hr
    each item in page.deps
      h3
        a(href=item.path)= item.title