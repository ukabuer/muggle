<!DOCTYPE html>
html(lang="en")
  head
    title= site.title
  body
    include ../common/nav.pug
    br
    h1 Articles
    hr
    each item in page.deps
      h3
        a(href=item.path)= item.title
