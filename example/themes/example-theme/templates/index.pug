<!DOCTYPE html>
html(lang="en")
  head
    title= site.title
    meta(charset="utf-8")
  body
    include common/nav.pug
    br
    h1= page.title
    h2= page.slogan
    hr
    p We have #{page.deps.length} articles